{
  "name": "upimages",
  "version": "1.0.0",
  "scripts": {
    "server": "cd backend && nodemon server.js",
    "client": "cd frontend && npm start",
    "t": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.1.0"
  }
}
